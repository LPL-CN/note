字符串截取:
	${变量名:起始位置:截取长度}

字符串替换:
	${变量名/old/new}		#替换一个匹配字符串
	${变量名//old/new}		#替换全部匹配字符串

字符串掐头:
	${变量名#*关键词}		#从头开始删到最近匹配关键词
	${变量名##*关键词}		#从头开始删到最远匹配关键词

字符串去尾:
	${变量名%关键词*}		#从尾开始删到最近匹配关键词
	${变量名%%关键词*}		#从尾开始删到最远匹配关键词

字符串赋予初值:
	${变量名:-***}			#当变量为空时,变量的值为初值
